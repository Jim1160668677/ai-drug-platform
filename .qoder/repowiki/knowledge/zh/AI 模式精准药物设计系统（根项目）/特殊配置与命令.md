环境创建：`conda env create -f environment.yml && conda activate pdd-system`；初始化种子用户：`python -m backend.app.db.init_db founder@pdd.dev password123`；启动后端：`uvicorn backend.app.main:app --host 0.0.0.0 --port 8000 --reload`；启动前端：`streamlit run frontend/app.py --server.port 8501`；运行全部测试并生成 HTML 覆盖率：`pytest --cov=backend/app --cov-report=html`。