# 八大功能模块增强 — 续作计划

> 基于代码库实际状态探索后制定。Phase 1 已完成（Agnes 集成 + ERR_ABORTED 修复 + pytest 修复），本计划聚焦剩余 Phase 2-8 的实施。

## 摘要

本计划基于对实际代码库的探索，明确已完成与未完成的工作：

- **Phase 1**：✅ 已完成（Agnes 集成、ERR_ABORTED 修复、pytest 5 个 LLM 测试修复、API 连通性验证）
- **Phase 2**：🔄 部分完成（bio_analyzer.py 已增强；data_io.py 需增加 Excel 支持；data.py 端点已存在需扩充）
- **Phase 3**：⬜ 待实施（molecule_designer.py 有 design_multi_target 但缺 DiffDock/LLM/structure_image）
- **Phase 4**：⬜ 待实施（ClinicalFeedback 模型已存在；feedback_loop.py 已有 FeedbackLoop；缺患者反馈服务层+端点）
- **Phase 5**：⬜ 待实施（federated_learning.py 有 FedAvg+MAD；缺 metrics_history/DP/multi-center/Redis）
- **Phase 6**：⬜ 待实施（hypothesis_generator.py 有 5 条规则；缺 LLM 集成；hypotheses.py 端点已存在）
- **Phase 7**：⬜ 待实施（discovery_pipeline.py 有 3 步；缺 Step 4 假设生成 + custom_steps；pipeline.py 端点已存在）
- **Phase 8**：⬜ 待实施（GitHub 库集成 + 全面测试）

## 当前状态分析（基于探索）

### 已确认完成的文件

| 文件 | 完成内容 | 探索证据 |
|---|---|---|
| `backend/app/services/analyzer/bio_analyzer.py` | gseapy 集成 + plot_data + random_state + parameters | L196-L255 调用 gseapy.enrichr；所有方法返回 plot_data 和 parameters |
| `backend/.env` | Agnes 配置 | USE_MOCK=false, LLM_BASE_URL=https://apihub.agnes-ai.com/v1, LLM_MODEL_FAST=agnes-2.0-flash |
| `frontend/middleware.ts` | 服务端 cookie 守卫 | PROTECTED_PREFIXES + TOKEN_COOKIE 检查 |
| `backend/app/db/seed.py` | Agnes LLM 配置灌入 | seed_llm_config() 插入 provider=agnes |
| `backend/app/services/llm/router.py` | LLMRouter.complete 流程 | 输入护栏→缓存→LLM→输出护栏→成本→缓存写入 |
| `backend/tests/test_llm_modules_coverage.py` | 5 个 LLM 测试修复 | _reset_llm_singletons 重置缓存单例；模型名从 settings 读取 |

### 已存在但需增强的文件

| 文件 | 现有 | 缺失 |
|---|---|---|
| `backend/app/services/analyzer/data_io.py` (137 行) | CSV/JSON/FASTA/VCF/MTX 导入 + CSV/JSON 导出 | Excel 导入导出 |
| `backend/app/services/analyzer/molecule_designer.py` (938 行) | design_multi_target + assess_druglikeness + predict_admet + explain_molecule + DeepChem 降级 | DiffDock 真实对接、LLM 辅助设计、structure_image (base64 PNG)、target_affinities 改为列表格式 |
| `backend/app/services/knowledge/hypothesis_generator.py` (285 行) | 5 条规则 + generate() + _collect_evidence | LLM 集成 (generate_with_llm)、hybrid 模式、假设追踪状态 |
| `backend/app/services/optimizer/federated_learning.py` (322 行) | FedAvg + MAD + 客户端注册 + 学习率衰减 + Flower 检测 | metrics_history、差分隐私 (DP-SGD)、多中心层级聚合、Redis 持久化 |
| `backend/app/services/orchestrator/discovery_pipeline.py` (474 行) | 3 步流水线 (target→molecule→treatment) | Step 4 假设生成、custom_steps 参数 |
| `backend/app/services/workflow/feedback_loop.py` (703 行) | FeedbackLoop + ExperimentTracker + LimsImporter | 患者反馈专用服务（独立于实验反馈） |

### 已存在且无需修改的端点

| 端点文件 | 现有端点 |
|---|---|
| `backend/app/api/v1/endpoints/hypotheses.py` (263 行) | CRUD + /compare + /analyze + /merge + /run-analysis + /eliminate + /auto-generate |
| `backend/app/api/v1/endpoints/pipeline.py` (115 行) | POST /run + GET /status/{project_id} |
| `backend/app/api/v1/endpoints/federated.py` (261 行) | jobs CRUD + /weights + /clients/register + /clients |
| `backend/app/api/v1/endpoints/data.py` (406 行) | /upload + /import + /{id}/parse + /{id}/quality + /{id}/process + /{id}/umap + /{id}/markers + /{id}/analyze + /{id}/export |

### 已存在的数据模型

| 模型 | 表名 | 关键字段 |
|---|---|---|
| `ClinicalFeedback` (treatment.py L53) | clinical_feedbacks | treatment_id, patient_code, age, gender, dosage, duration_days, efficacy, adverse_reactions (JSON), biomarker_changes (JSON), notes |
| `DockingResult` (molecule.py L38) | docking_results | molecule_id, protein_pdb_id, protein_pdb_path, poses (JSON), top_confidence, docked_by |
| `Hypothesis` (hypothesis.py L21) | hypotheses | name, description, mechanism, strategy, analysis_config, status, target_list, analysis_result, forced_deep_analysis, force_reason |
| `HypothesisAnalysis` (hypothesis.py L47) | hypothesis_analyses | hypothesis_id, analysis_tier, summary, result_data |

## 实施步骤

### Phase 2: 完成 data_io.py Excel 支持

#### Step 2.1：增强 data_io.py
**修改文件**：`backend/app/services/analyzer/data_io.py`

新增方法：
- `import_excel(content: bytes) -> Dict` — 使用 openpyxl 读取多 sheet Excel
- `export_excel(data: Dict) -> bytes` — 使用 openpyxl 写入 Excel

降级策略：openpyxl 未安装时抛出明确错误提示安装，不降级（Excel 是可选功能）

```python
@staticmethod
async def import_excel(content: bytes) -> Dict[str, Any]:
    """Excel 多 sheet 导入"""
    try:
        from openpyxl import load_workbook
    except ImportError:
        return {"error": "openpyxl 未安装，请运行 pip install openpyxl"}
    wb = load_workbook(io.BytesIO(content), read_only=True)
    sheets = {}
    for sheet_name in wb.sheetnames:
        ws = wb[sheet_name]
        rows = list(ws.iter_rows(values_only=True))
        if rows:
            header = [str(c) if c else f"col_{i}" for i, c in enumerate(rows[0])]
            data = []
            for row in rows[1:]:
                data.append(dict(zip(header, row)))
            sheets[sheet_name] = {"headers": header, "rows": data}
    return {"sheets": sheets, "format": "excel"}

@staticmethod
async def export_excel(data: Dict[str, Any]) -> bytes:
    """Excel 导出 — 分析结果写入单 sheet"""
    try:
        from openpyxl import Workbook
    except ImportError:
        raise ImportError("openpyxl 未安装，请运行 pip install openpyxl")
    wb = Workbook()
    ws = wb.active
    ws.title = "analysis_result"
    # 根据 data 类型写入（genes/clusters/pathways）
    if "genes" in data:
        ws.append(["gene", "log2fc", "pvalue", "padj", "regulation", "significant"])
        for g in data["genes"]:
            ws.append([g.get("gene"), g.get("log2fc"), g.get("pvalue"),
                       g.get("padj"), g.get("regulation"), g.get("significant")])
    elif "clusters" in data:
        ws.append(["gene", "cluster_id", "pca_x", "pca_y"])
        for c in data["clusters"]:
            ws.append([c.get("gene"), c.get("cluster_id"), c.get("pca_x"), c.get("pca_y")])
    elif "pathways" in data:
        ws.append(["id", "name", "pvalue", "ratio", "genes"])
        for p in data["pathways"]:
            ws.append([p.get("id"), p.get("name"), p.get("pvalue"),
                       p.get("ratio"), ";".join(p.get("genes", []))])
    output = io.BytesIO()
    wb.save(output)
    return output.getvalue()
```

同步更新 `detect_format()` 增加 xlsx/xls 识别，更新 `data.py` 的 `/import` 和 `/export` 端点支持 Excel。

#### Step 2.2：更新 data.py 端点支持 Excel
**修改文件**：`backend/app/api/v1/endpoints/data.py`

- `ALLOWED_EXTENSIONS` 增加 "xlsx", "xls"
- `DATA_TYPE_MAP` 增加 xlsx/xls → RNA_SEQ
- `/import` 端点增加 Excel 路由
- `/{dataset_id}/export` 端点 format 参数增加 "excel" 选项

### Phase 3: 多靶点协同分子设计完善

#### Step 3.1：DiffDock 对接（Mock + Real 双模式）
**修改文件**：`backend/app/services/analyzer/molecule_designer.py`

新增方法 `dock_molecule(smiles, target_pdb_id)`：
- Mock 模式：返回随机亲和力 + 模拟 pose（基于现有 _compute_affinity 扩展）
- Real 模式（NVIDIA_NIM_API_KEY 非空时）：调用 DiffDock NIM API
- 返回：`{affinity: float, binding_pose: dict, confidence: float, source: "diffdock"/"mock"}`
- 写入 DockingResult 表（已存在）

```python
async def dock_molecule(self, smiles: str, target_pdb_id: str = "") -> Dict[str, Any]:
    """分子对接 — DiffDock Mock/Real 双模式"""
    from app.core.config import settings
    nim_key = getattr(settings, "NVIDIA_NIM_API_KEY", "")
    if nim_key:
        try:
            async with httpx.AsyncClient(timeout=60) as client:
                resp = await client.post(
                    "https://integrate.api.nvidia.com/v1/diffdock",
                    headers={"Authorization": f"Bearer {nim_key}"},
                    json={"ligand": smiles, "receptor": target_pdb_id},
                )
                resp.raise_for_status()
                data = resp.json()
                return {
                    "affinity": float(data.get("affinity", -7.0)),
                    "binding_pose": data.get("pose", {}),
                    "confidence": float(data.get("confidence", 0.7)),
                    "source": "diffdock",
                }
        except Exception as e:
            logger.warning(f"DiffDock API 失败，降级 Mock: {e}")
    # Mock 模式
    import random
    return {
        "affinity": round(-random.uniform(6, 11), 2),
        "binding_pose": {"rmsd": round(random.uniform(0.5, 3.0), 2)},
        "confidence": round(random.uniform(0.5, 0.9), 2),
        "source": "mock",
    }
```

#### Step 3.2：LLM 辅助分子设计
**修改文件**：`backend/app/services/analyzer/molecule_designer.py`

新增方法 `async llm_assisted_design(targets, constraints, llm_client)`：
- 构造 prompt：靶点信息 + 约束 + 已有分子反馈
- 调用 LLM 生成 SMILES + 设计理由
- 解析响应，调用 assess_druglikeness 验证
- 失败降级为规则生成

集成到 `design_multi_target()`：通过 `use_llm: bool = True` 参数控制

#### Step 3.3：增强多靶点设计返回结构
**修改文件**：`backend/app/services/analyzer/molecule_designer.py`

修改 `design_multi_target()` 返回结构，每个分子包含：
- `structure_image`: base64 PNG（RDKit Draw.MolDraw2DCairo 绘制，rdkit 不可用时为 None）
- `target_affinities`: 改为**列表**格式（而非 dict），每个靶点单独成行：
  ```python
  [{"target_id": "...", "gene_symbol": "EGFR", "affinity": -9.5, "confidence": 0.85}, ...]
  ```
- `design_rationale`: LLM 生成的设计理由（如未启用 LLM 则为空字符串）

#### Step 3.4：前端分子展示增强
**修改文件**：`frontend/app/workbench/molecules/page.tsx`

- 分子列表表格：增加列「靶点结合亲和力」（多靶点用 Tag 展示）
- 分子详情抽屉：增加「靶点亲和力表」（每个靶点一行）
- 分子结构图：使用 rdkit.js 或后端生成的 base64 PNG
- 多靶点设计弹窗：增加「使用 LLM 辅助」+「使用 DiffDock 对接」开关

### Phase 4: 干湿实验闭环完善（患者用药反馈）

#### Step 4.1：患者反馈 Schema
**新建文件**：`backend/app/schemas/patient_feedback.py`

Pydantic 模型：
- `PatientInfoSchema`: age, gender, diagnosis, stage, biomarkers
- `MedicationPlanSchema`: drug_name, dose, unit, route, frequency, duration_days
- `EfficacySchema`: response_criteria (RECIST), tumor_shrinkage_pct, pfs_days, os_days
- `AdverseEventSchema`: event_name, severity (CTCAE 1-5), action_taken, outcome
- `PatientFeedbackCreate`: treatment_id + 上述组合（映射到 ClinicalFeedback 模型字段）
- `PatientFeedbackResponse`: 上述 + id, created_at

#### Step 4.2：患者反馈服务层
**新建文件**：`backend/app/services/workflow/patient_feedback.py`

类 `PatientFeedbackService`：
- `async create(db, payload) -> ClinicalFeedback`: 创建反馈记录（字段映射）
- `async list_by_treatment(db, treatment_id) -> list`: 按治疗方案查询
- `async list_by_target(db, target_symbol) -> list`: 按靶点查询（通过 Treatment.target_ids 关联）
- `async get_statistics(db, target_symbol) -> dict`: 统计有效率（complete+partial）/总例数、不良反应率
- `async link_to_prediction(db, feedback_id, experiment_id)`: 关联干实验预测

#### Step 4.3：患者反馈端点
**修改文件**：`backend/app/api/v1/endpoints/feedback.py`（如不存在则新建）

端点（注册到 router.py）：
- `POST /api/v1/feedback/patient`: 提交患者用药反馈
- `GET /api/v1/feedback/patient`: 查询患者反馈列表（支持 treatment_id、target_symbol 过滤）
- `GET /api/v1/feedback/patient/{id}`: 反馈详情
- `GET /api/v1/feedback/patient/stats/{target_symbol}`: 按靶点统计有效率/不良反应率
- `GET /api/v1/feedback/template`: 下载标准化数据采集模板（CSV）

#### Step 4.4：标准化数据采集模板
在 `patient_feedback.py` 服务中增加 `generate_template(format: str) -> bytes`：
- CSV 模板：表头 + 示例行（patient_code, age, gender, diagnosis, drug_name, dose, duration_days, efficacy, adverse_events）
- 用于临床医生批量录入

### Phase 5: 联邦学习全面完善

#### Step 5.1：性能监控与评估
**修改文件**：`backend/app/services/optimizer/federated_learning.py`

- job 数据结构增加 `metrics_history: List[Dict]`
- `_aggregate()` 方法每轮记录：global_loss, global_accuracy, val_metrics, client_contributions, duration_sec
- 新增方法 `async get_metrics_history(job_id) -> List[Dict]`
- 新增方法 `async evaluate_global_model(job_id, val_dataset) -> Dict`（Mock 返回随机指标）

#### Step 5.2：差分隐私
**修改文件**：`backend/app/services/optimizer/federated_learning.py`

新增 `DPSGDOptimizer` 类：
- `add_noise(weights, noise_multiplier, max_norm)`: 梯度裁剪 + 高斯噪声
- job 配置增加 `dp_params: {enabled, noise_multiplier, max_grad_norm, target_epsilon}`
- 在 `_aggregate()` 中如果 dp_params.enabled 则对权重添加噪声
- 优先使用 opacus（如已安装），否则手动实现

```python
class DPSGDOptimizer:
    @staticmethod
    def add_noise(weights: Dict[str, float], noise_multiplier: float = 0.1, max_norm: float = 1.0) -> Dict[str, float]:
        """梯度裁剪 + 高斯噪声添加"""
        import random
        clipped = {}
        for k, v in weights.items():
            # 裁剪
            v_clipped = max(-max_norm, min(max_norm, v))
            # 添加噪声
            noise = random.gauss(0, noise_multiplier * max_norm)
            clipped[k] = v_clipped + noise
        return clipped
```

#### Step 5.3：多中心协作模式
**修改文件**：`backend/app/services/optimizer/federated_learning.py`

- job 配置增加 `centers: List[{id, name, client_ids}]`
- 新增 `_hierarchical_aggregate(submissions, centers)`: 先中心内聚合，再中心间聚合
- 在 `submit_weights()` 中检测 centers 配置，启用层级聚合

#### Step 5.4：Redis 持久化（可选）
**修改文件**：`backend/app/services/optimizer/federated_learning.py`

- 新增 `RedisFLStorage` 类：save_job, load_job, list_jobs, save_metrics
- 在 `__init__` 中检测 Redis 可用性，可用则启用，不可用降级内存
- 失败时自动降级，保持向后兼容

#### Step 5.5：增强 federated.py 端点
**修改文件**：`backend/app/api/v1/endpoints/federated.py`

新增端点：
- `GET /jobs/{job_id}/metrics`: 获取性能监控历史
- `POST /jobs/{job_id}/dp`: 启用/禁用差分隐私
- `GET /jobs/{job_id}/centers`: 多中心配置

### Phase 6: 假设生成模块完善

#### Step 6.1：LLM 集成假设生成
**修改文件**：`backend/app/services/knowledge/hypothesis_generator.py`

新增方法 `async generate_with_llm(context, llm_client) -> List[Dict]`：
1. 收集上下文：DE genes、pathways、molecules、clinical_feedbacks、clusters
2. 构造 prompt（中文，要求 JSON 输出）
3. 调用 LLM，解析 JSON 响应
4. 与规则推理结果合并，去重

新增方法 `async generate_hybrid(project_id, context, llm_client) -> List[Dict]`：
- 同时调用规则推理和 LLM 生成
- 合并、去重、按置信度排序

#### Step 6.2：假设追踪状态扩展
**修改文件**：`backend/app/services/knowledge/hypothesis_generator.py`

假设数据结构增加字段：
- `source`: "rule" / "llm" / "hybrid"
- `related_targets`: List[str]
- `related_molecules`: List[str]
- `tested_in_pipeline`: Optional[str] (关联的 pipeline run_id)

新增方法：
- `async update_status(hypothesis_id, status, result)`: 更新假设状态
- `async get_hypothesis(hypothesis_id)`: 查询单个假设

#### Step 6.3：增强 hypotheses.py 端点
**修改文件**：`backend/app/api/v1/endpoints/hypotheses.py`

修改 `/auto-generate` 端点增加参数：
- `mode: str = "rule"` (rule/llm/hybrid)
- 启用 LLM 时通过 `get_llm_client_with_config(db)` 获取客户端

新增端点：
- `PATCH /{hypothesis_id}/status`: 更新假设状态
- `GET /{hypothesis_id}/validation`: 获取验证方法详情

### Phase 7: 一键流水线集成假设生成

#### Step 7.1：流水线增加 Step 4 假设生成
**修改文件**：`backend/app/services/orchestrator/discovery_pipeline.py`

新增方法 `_step4_generate_hypotheses(project_id, targets, molecules, treatments)`：
- 调用 HypothesisGenerator.generate_hybrid()
- 通过 get_llm_client_with_config 获取 LLM 客户端
- 返回 `{status, hypotheses_generated, hypotheses}`

修改 `run()` 方法：在 Step 3 之后增加 Step 4 调用

#### Step 7.2：支持自定义步骤
**修改文件**：`backend/app/services/orchestrator/discovery_pipeline.py`

`run()` 方法增加 `custom_steps: Optional[List[str]] = None` 参数：
- 默认执行 `["target_discovery", "molecule_generation", "treatment_matching", "hypothesis_generation"]`
- custom_steps 非空时按指定顺序执行
- 每步失败不中断后续步骤（容错）
- 返回结果中增加 `executed_steps` 字段

```python
AVAILABLE_STEPS = {
    "target_discovery": "_step1_discover_targets",
    "molecule_generation": "_step2_generate_molecules",
    "treatment_matching": "_step3_match_treatments",
    "hypothesis_generation": "_step4_generate_hypotheses",
}
```

#### Step 7.3：增强 pipeline.py 端点
**修改文件**：`backend/app/api/v1/endpoints/pipeline.py`

- `PipelineRunRequest` 增加 `custom_steps: Optional[List[str]]` 和 `use_llm_for_hypotheses: bool = True`
- 新增 `GET /steps`: 列出所有可用步骤
- `run()` 端点传递 custom_steps 参数

### Phase 8: GitHub 集成 + 全面测试

#### Step 8.1：安装成熟库
```powershell
cd g:\软件开发\AI药物\backend
pip install openpyxl gseapy rdkit-pypi opacus 2>&1 | Select-Object -Last 10
```
- `openpyxl`: Excel 读写（Phase 2）
- `gseapy`: 通路富集（已在 bio_analyzer.py 集成，需安装）
- `rdkit-pypi`: 分子操作（SMILES 解析、类药性、绘图）
- `opacus`: 差分隐私（Phase 5）

注意：deepchem 依赖 TensorFlow，安装可能失败 — 保持现有 Mock 降级即可。flower 可选安装。

#### Step 8.2：验证 gseapy 集成
验证 bio_analyzer.py 的 pathway_enrichment() Real 模式可调用 gseapy.enrichr()

#### Step 8.3：验证 rdkit 集成
验证 molecule_designer.py 的 assess_druglikeness() 和 explain_molecule() 真实模式可用

#### Step 8.4：全面功能测试
```powershell
cd g:\软件开发\AI药物\backend
# 后端单元测试（覆盖率 ≥ 80%）
pytest tests/ --cov=app --cov-report=term-missing 2>&1 | Select-Object -Last 50
```

```powershell
cd g:\软件开发\AI药物\frontend
# 前端测试 + 类型检查
npm run test 2>&1 | Select-Object -Last 30
npx tsc --noEmit 2>&1 | Select-Object -Last 20
```

#### Step 8.5：端到端集成测试
1. 登录 → 创建项目 → 上传数据 → 生信分析 → 查看可视化
2. 多靶点分子设计 → DiffDock 对接（Mock）→ 查看亲和力表
3. 患者反馈录入 → 按靶点统计 → 下载模板
4. 联邦学习任务创建 → 性能监控 → 差分隐私开关
5. 假设生成（rule + llm hybrid）→ 状态更新
6. 一键流水线（含 Step 4 假设生成，custom_steps）

#### Step 8.6：性能测试
- API 响应时间：关键端点 < 2s（LLM 端点 < 30s）
- 并发：10 并发用户无错误
- 数据库：1万条记录查询 < 500ms

#### Step 8.7：兼容性 + 安全性测试
- 浏览器：Chrome / Edge / Firefox
- 鉴权：未登录访问受保护端点返回 401
- 越权：用户 A 不能访问用户 B 的数据
- SQL 注入 / XSS 防护验证

## 假设与决策

1. **Phase 1 已完成**：基于前次会话总结，Agnes 集成、ERR_ABORTED 修复、pytest 修复均已完成，不再重复。
2. **bio_analyzer.py 已增强**：探索验证 gseapy/plot_data/random_state/parameters 已到位。
3. **ClinicalFeedback 模型已存在**：Phase 4 直接复用，不新建模型。
4. **DockingResult 模型已存在**：Phase 3 的 DiffDock 结果可直接写入此表。
5. **hypotheses.py / pipeline.py / federated.py 端点已存在**：增强而非新建。
6. **DiffDock Mock/Real 双模式**：NVIDIA_NIM_API_KEY 为空时用 Mock，非空时调用真实 API。
7. **LLM 辅助可选**：通过 `use_llm: bool` 参数控制，失败降级为规则推理。
8. **GitHub 库降级**：deepchem/gseapy/opacus 不可用时降级为 Mock。
9. **数据库迁移**：使用 init_db.py 的 create_all()，不使用 Alembic（项目未配置）。如需新增表（如 PatientFeedback 专用表），通过新增模型类 + init_db 重建。
10. **测试账号**：`sid@ai-drug.com` / `demo123456`（seed.py 中 DEMO_PASSWORD）。

## 验证步骤

### Phase 2 验证
- [ ] `data_io.py` 的 `import_excel()` 和 `export_excel()` 可调用
- [ ] `data.py` 的 `/import` 和 `/export` 支持 Excel 格式
- [ ] openpyxl 安装成功

### Phase 3 验证
- [ ] `dock_molecule()` Mock 模式返回 affinity/confidence
- [ ] `llm_assisted_design()` 可调用并返回 SMILES + rationale
- [ ] `design_multi_target()` 返回结构包含 structure_image、target_affinities (列表格式)
- [ ] 前端分子列表显示靶点亲和力

### Phase 4 验证
- [ ] `PatientFeedbackService.create()` 可创建反馈记录
- [ ] `POST /api/v1/feedback/patient` 可提交反馈
- [ ] `GET /api/v1/feedback/patient/stats/{target}` 返回统计
- [ ] 标准化模板可下载

### Phase 5 验证
- [ ] 联邦学习 job 包含 `metrics_history`
- [ ] 差分隐私开关可启用（dp_params.enabled）
- [ ] 多中心配置生效（centers 字段）
- [ ] `GET /jobs/{job_id}/metrics` 返回监控历史

### Phase 6 验证
- [ ] `generate_with_llm()` 可调用 LLM 生成假设
- [ ] `generate_hybrid()` 合并规则+LLM 结果
- [ ] 假设包含 source/related_targets/tested_in_pipeline 字段
- [ ] `/auto-generate` 端点支持 mode 参数

### Phase 7 验证
- [ ] 流水线 Step 4 生成假设
- [ ] `custom_steps` 参数生效（可指定执行顺序）
- [ ] `GET /api/v1/pipeline/steps` 返回步骤列表
- [ ] 端到端流水线无错误

### Phase 8 验证
- [ ] openpyxl/gseapy/rdkit/opacus 安装成功（或降级 Mock）
- [ ] pytest 覆盖率 ≥ 80%
- [ ] 前端测试通过
- [ ] 端到端流程无错误
- [ ] 安全测试通过

## 执行顺序

1. **Phase 2**（data_io.py Excel 支持）— 快速完成
2. **Phase 3**（多靶点分子设计完善）— DiffDock + LLM + structure_image
3. **Phase 4**（患者反馈闭环）— Schema + Service + Endpoints
4. **Phase 5**（联邦学习完善）— metrics + DP + multi-center
5. **Phase 6**（假设生成 LLM 集成）— generate_with_llm + hybrid
6. **Phase 7**（流水线 Step 4 + custom_steps）— 依赖 Phase 6
7. **Phase 8**（GitHub 集成 + 全面测试）— 依赖所有前序 Phase

## 风险与缓解

1. **Agnes API 不可用**：降级为 Mock 模式，记录警告日志
2. **openpyxl/rdkit 安装失败**：保持现有降级路径，Excel 功能不可用时返回明确错误
3. **deepchem 安装失败**（依赖 TensorFlow）：降级为 rdkit + 手动实现，跳过 deepchem
4. **端口冲突**：后端 8000 / 前端 3000，如被占用先检查
5. **测试隔离问题**：conftest.py 强制 USE_MOCK=true + 内存数据库
6. **LLM 调用超时**：设置 30s 超时，失败降级为规则推理
