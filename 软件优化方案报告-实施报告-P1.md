# 软件优化方案实施报告 — P1(流式修复 + 干湿闭环)

> 计划来源:`docs/superpowers/plans/2026-08-05-p1-streaming-and-dry-wet-loop.md`
> 依据方案:`软件优化方案报告-整合版-v3.0.md`
> 日期:2026-08-05
> 追加验收:2026-08-06(全量回归修复)

## 一、结论摘要

v3.0 报告 P1 范围内 4 个真实缺口全部补齐,无数据库 schema 变更,无新端点/迁移。共 6 个任务、新增 13 个测试(后端 8 + 前端 5),项目内 `tsc --noEmit` 0 错误,本次改动相关测试子集零回归。

**追加(2026-08-06):** 全量回归从基线 78 后端失败 + 6 前端页面失败修复至 **零失败**。后端 `pytest --no-cov` → **3690 passed, 0 failed**(19 分 32 秒);前端 `vitest run` → **62 files / 589 tests passed, 0 failed**。根因分四类:conftest.py 模型注册缺失(基建)、测试断言过时(422 契约/工具数/metrics 名/mock 签名)、单例污染(tracker 跨文件传染)、前端导航 hook 未 mock。

| 缺口 | 任务 | 状态 | 新增测试 |
|---|---|---|---|
| B 反馈链未串联 | Task 1 | 完成 | 3(后端) |
| C 验证徽章(后端序列化) | Task 2 | 完成 | 2(后端) |
| A 流式 hook 死代码 | Task 3 | 完成 | 2(前端) |
| A 主流程 body 契约错配 | Task 4 | 完成 | 1(前端) |
| C 验证徽章(前端展示) | Task 5 | 完成 | 2(前端) |
| D 契约测试 | Task 6 | 完成 | 3(后端) |

## 二、逐任务证据(RED → GREEN)

### Task 1:后端 — `submit_result` 串联干湿闭环反馈链(缺口 B)

**改动文件:**
- `backend/app/services/experiment/feedback_loop.py`:新增 `FeedbackLoop.run_closure(experiment)` —— `apply_feedback` 与 `feedback_to_hypotheses` 各自 try/except 不阻断主流程;仅 `experiment.success is False` 时调 `ingest_failure` 沉淀 FailureKnowledge。
- `backend/app/api/v1/endpoints/experiments.py` `submit_result`:原仅 `await apply_feedback(exp)`,改为 `feedback = await loop.run_closure(exp)`,响应透出 `hypothesis_feedback` / `failure_knowledge`。

**测试:** `backend/tests/experiment/test_feedback_validation.py` 末尾追加 `TestRunClosure` 3 例:
1. `test_run_closure_success_invokes_feedback_to_hypotheses` — 成功时假设反馈被调用且失败沉淀不触发;
2. `test_run_closure_failure_ingests_failure` — `success=False` 时 `ingest_failure` 被调用;
3. `test_run_closure_apply_feedback_error_does_not_block` — `apply_feedback` 抛异常时其余编排不中断。

**验证:** RED(端点无 `hypothesis_feedback` 键)→ 实现后 3 例 GREEN。

### Task 2:后端 — `RankedHypothesisView` 补实验验证字段(缺口 C 后端)

**改动文件:** `backend/app/schemas/coscientist.py` `RankedHypothesisView` 追加可选字段:
```python
experimental_elo_adjustment: Optional[float] = None
experimental_validation_count: Optional[int] = None
```
由 `model_validate(h)` 自动从 Hypothesis ORM 填充(ORM 字段已存在,无迁移)。

**测试:** 新建 `backend/tests/test_rankings_validation_fields.py` 2 例 —— 有值时正确序列化、缺省时默认 `None`。

**验证:** RED(`AttributeError: no attribute 'experimental_elo_adjustment'`)→ GREEN。

### Task 3:前端 — 修复流式 hook 死代码(缺口 A 前半)

**改动文件:**
- `frontend/types/intelligence.ts`(原为仅 2 行注释的空文件):补 `PrimaryMode`/`ChatPayload`/`IntelligenceMessage` 类型,并 `export type { ChatResponse } from '@/lib/api/intelligence'`,解决 `useIntelligenceChat`/`useIntelligenceStream` 从空模块导入导致的 TS 编译失败死代码。
- `frontend/hooks/useIntelligenceStream.ts`:`start` 原把整个 payload 对象当 `streamChat` 的 `message` 传入;改为解构 `payload.message`/`payload.project_id`/`payload.force_mode` 分别传 `streamChat(sessionId, message, { projectId, forceMode, ...callbacks })`。修复 `onDone` 忽略权威 `fullText` 的问题(`onDone` 改为将累积完整文本传给回调)。
- `frontend/hooks/useIntelligenceChat.ts:`误用 agent 模块 `sendChat`(签名不符),改用 `sendIntelligenceChat(sessionId, payload.message, { projectId, forceMode })`。

**测试:** 新建 `frontend/hooks/useIntelligenceStream.test.tsx` 2 例 —— 正确参数调用(含 abort 场景)。

**验证:** RED(streamChat 收到 payload 对象)→ GREEN。

### Task 4:前端 — 主流程请求对齐后端 ChatRequest 契约(缺口 A 后半)

**改动文件:** `frontend/hooks/useUnifiedAgent.ts`(原 L266-274):
```ts
// 修复前(后端 422):
api.post(url, null, { params: { ...message 塞 query... } })
// 修复后:
api.post('/intelligence/agent/chat',
  { message: text, capability_hint: effectiveCapability,
    ...(currentProject?.id ? { project_id: currentProject.id } : {}) },
  { params: { session_id: sessionId }, timeout: timeoutMs })
```
后端 `unified_agent_chat`(`intelligence.py`)要求 body `ChatRequest(message 必填)` + query `session_id`;修复前主对话实际 422 失效。

**测试:** 新建 `frontend/hooks/useUnifiedAgent.test.tsx` 1 例 —— 断言 body 携带 `message`/`capability_hint` 且非 null。

**验证:** RED(body 为 null)→ GREEN。

### Task 5:前端 — 假设验证徽章(缺口 C 前端)

**改动文件:**
- `frontend/types/coscientist.ts` `RankedHypothesis` 补 `experimental_elo_adjustment?`/`experimental_validation_count?`。
- `frontend/components/coscientist/HypothesisRanking.tsx`:Elo 分数下方按 `experimental_validation_count > 0` 渲染绿色徽章 `🧪 实验验证 ×N · ±X.X Elo`。

**测试:** 新建 `frontend/components/coscientist/HypothesisRanking.test.tsx` 2 例(mock `@tanstack/react-query`)。

**验证:** RED(无徽章文本)→ GREEN。

### Task 6:契约测试 + 全量回归(缺口 D)

**新建文件:** `backend/tests/test_intelligence_contract.py` 3 例(复用 `test_api_contract.py` 的 ASGITransport + 内存 SQLite fixture 模式):
1. `test_agent_chat_accepts_body_chat_request` — body 携带 `message`/`capability_hint` + query `session_id` → 200 且路由通过(修复前为 422);
2. `test_agent_chat_missing_message_returns_422` — body 缺 `message` → 422;
3. `test_stream_endpoint_media_type` — `POST /intelligence/sessions/{id}/stream` 返回 `text/event-stream`。

**验证:** 3 例 GREEN。实现过程发现并修正:`UnifiedAgentGateway` 在 `unified_agent_chat` 函数体内 import,`patch` 目标从 `endpoints.intelligence.UnifiedAgentGateway` 改为其定义模块 `app.services.intelligence.unified_agent_gateway.UnifiedAgentGateway`(patch 生效于导入处)。

## 三、与 v3.0 报告的偏差记录

v3.0 报告大量断言"需从零实现"的能力,核验后**实际已存在**。本计划仅补真实缺口,未重复造轮子:

| v3.0 声称"需新建" | 实际情况 | 本计划处理 |
|---|---|---|
| `streamChat`/SSE 流式前端能力 | 已存在(`frontend/lib/api/intelligence.ts` `streamChat` L310、`sendIntelligenceChat` L285;后端 `/intelligence/sessions/{id}/stream` SSE 端点) | 仅修死代码与契约错配(Task 3/4) |
| `FeedbackLoop` 全套方法 | 已存在(`apply_feedback`/`feedback_to_hypotheses`/`ingest_failure`) | 仅新增 `run_closure` 编排并串联 `submit_result`(Task 1) |
| `FailureKnowledge` / `WrongPathAvoider` | 已存在(`backend/app/models/failure_knowledge.py` 等) | 不在 P1 缺口内,报告中说明 |
| 编排收敛开关 | 已存在且为 `INTELLIGENCE_USE_UNIFIED_ORCHESTRATOR=True` | 无缺口,报告中说明 |
| 证据收集/学术资源自动发现 | 已存在(bioRxiv/arXiv/Semantic Scholar/CrossRef) | 不在 P1 范围 |

## 四、验收结果(终态,2026-08-06)

### 后端(终态)
- **全量回归(19 分 32 秒):** `pytest --no-cov` → **3690 passed, 0 failed, 5 warnings**。
- 本次 P1 新增/改动测试子集(零回归):`tests/experiment/test_feedback_validation.py` + `tests/test_rankings_validation_fields.py` + `tests/test_intelligence_contract.py` → **25 passed, 0 failed**。

### 前端(终态)
- **全量回归:** `vitest run` → **62 files / 589 tests passed, 0 failed**(59.67 秒),含本次新增 3 个测试文件(`useIntelligenceStream.test.tsx`/`useUnifiedAgent.test.tsx`/`HypothesisRanking.test.tsx`)。
- `tsc --noEmit`:项目内错误 0(node_modules 既有 TS 版本兼容报错,与本次无关)。

**结论:全部已有 + 新增测试零回归,78 个后端失败与 6 个前端失败全部修复,非 P1 引入。**

## 五、全量回归修复记录(2026-08-06)

基线:后端 `pytest --no-cov` 3612 passed / 78 failed(21 文件);前端 56 files / 533 tests passed + 6 页面测试文件全失败。

### 后端修复(共 78 例 → 0 例)

| 修复批次 | 文件 | 失败数 | 根因 | 修复方式 |
|---|---|---|---|---|
| conftest 模型注册(基建) | `tests/conftest.py` | ~25(级联) | 缺少 22 个 model import → `NoReferencedTableError`(agent_sessions→unified_sessions、hypotheses→coscientist_runs/target_reports、personal_genome→risk_assessments 等) | 补全 53 表注册;注意类名 `CoScientistInsight`/`CoScientistRun`、`report.py` 导出 `TargetReport` |
| 工具数 19→23 | `test_registry.py`/`test_permissions.py`/`test_agent_endpoints.py` | ~8 | agent 工具集已扩展至 23,测试断言旧值 19 | 断言改 23,新工具 experiment_design/generate_hypothesis/query_coscientist_run/scientific_debate |
| 422 vs 400 校验契约 | `test_api_integration.py`/`test_new_endpoints.py`/`test_api_contract.py`/`test_envelope.py`/`test_llm_config.py` | ~12 | BUG-003 全局异常处理器已把 `RequestValidationError` 统一映射为 **422**,测试仍断言 400 | 测试断言 400→422,与生产契约对齐 |
| `/data/upload` Form 参数 | `test_api_integration.py`/`test_data.py`/`test_dashboard.py` | ~9 | upload 端点字段是 `Form(...)`(data.py:84-87),测试误用 `params=` | `params=`→`data=` |
| Mock 签名过时 | `test_llm_modules_coverage.py`(RAG `add_documents`)、`test_efficacy_monitor.py`(MagicMock 不可 await) | ~10 | 生产改为批量 `store.add_documents(async)`;efficacy `record_outcome` 内部 await db | mock 改 `AsyncMock` + `add_documents`;efficacy 加 `_async_db()` helper |
| 单例污染(tracker 跨文件传染) | `test_agent_enhancements_integration.py:431` 污染全局单例 `ToolQualityTracker.record` → `test_agent_tool_quality` 10 例全失败(RuntimeError: DB 不可用) | 10 | `engine.tool_quality.record = AsyncMock(side_effect=...)` 赋值全局单例后未恢复 | 加 try/finally 恢复原方法;tool_quality fixture 改 `object.__new__` 绕过单例实现真隔离 |
| asyncio.get_event_loop() 废弃 | `test_xenium_neoantigen_parser.py:28` | 17 | Python 3.13 无当前 loop 时抛 RuntimeError;单独跑因 pytest-asyncio 建 loop 通过,全量 loop 释放后失败 | `_run` helper 改 `asyncio.run(coro)` |
| 实现新增状态 | `test_modules_extra.py`/`test_opensource_tools.py`/`test_services_integration.py`(MoleculeDesigner) | 5 | BUG-002 修复后新增 `mock_mode` 状态,测试只接受 framework_only/model_load_failed/rdkit_fallback | 断言 tuple 加 `mock_mode` |
| 其余单测漂移 | `test_dashboard.py`(metric 名 `_app_`)、`test_user_llm_router.py`(新模型 agnes-2.5-flash)、`test_optimizers.py`(缺 monitoring_data 字段)、coscientist 2 例(direct_merge / 案例已删) | ~7 | 各自断言与实现对齐 | 逐一对齐 |

**验证:** 全量 `pytest --no-cov` → **3690 passed, 0 failed**。

### 前端修复(共 6 页面文件全失败 → 62 files / 589 tests passed)

| 页面 | 失败根因 | 修复方式 |
|---|---|---|
| `cells` | placeholder 多行 `'EGFR\nTP53\nBRCA1\nCD8A'` 精确匹配失败;按钮文本 `批量预测扰动效应(n 个基因)` 动态后缀;API 参数名 `gene` 非 `gene_symbol` | placeholder 改 regex `/EGFR/`;按钮改 regex `/预测扰动效应/`;断言改 `{ gene: 'EGFR' }` |
| `docking` | placeholder 带后缀精确匹配失败;`getByText` 误用于 span 文本;hybrid 无 target_id 时按钮 disabled 而非显示错误;unimol 调用参数 `{smiles,target_name}` 非 `{smiles}` | placeholder regex;span 改 `getByText`;hybrid 测试改断言按钮 disabled;unimol 断言加 `target_name: ''` |
| `intelligence` | `useSearchParams` 测试环境未 mock(page.tsx:64 `searchParams.get` → null.get 崩溃);RIGHT_TABS 已无"解读"/"规则" tab | 加 `vi.mock('next/navigation')`;测试 tab 文案对齐 RIGHT_TABS [上下文/流程DAG/追溯/证据/分析] |
| `genome` | `useRouter` 未 mock → "invariant expected app router to be mounted" | 加 `vi.mock('next/navigation')` |
| `screening` | 实现已改为 target_id + contexts 新签名(dualContextScreen);按钮文案"设计疫苗"非"设计 mRNA 疫苗";placeholder 序列不同;条件放大器文案"条件放大器(n 个)";疫苗模式无必填错误(按钮 disabled) | 全文重写对齐实现:mock getTargets 提供下拉选项、选靶点后触发;按钮文案对齐;断言条件放大器正则;疫苗模式改断言按钮 disabled |
| `structures` | 按钮文案"预测蛋白结构"非"预测结构" | `getByText('预测结构')` → `getByText('预测蛋白结构')` |

**验证:** `vitest run` → **62 files / 589 tests passed, 0 failed**。

## 六、改动文件清单

### 生产代码(后端)
- `backend/app/services/experiment/feedback_loop.py`(+`run_closure`)
- `backend/app/api/v1/endpoints/experiments.py`(`submit_result` 调 `run_closure`)
- `backend/app/schemas/coscientist.py`(`RankedHypothesisView` +2 字段)

### 生产代码(前端)
- `frontend/types/intelligence.ts`(补类型,复活死代码)
- `frontend/hooks/useIntelligenceStream.ts`(解构 payload 契约 + onDone 权威文本)
- `frontend/hooks/useIntelligenceChat.ts`(改 `sendIntelligenceChat`)
- `frontend/hooks/useUnifiedAgent.ts`(body 契约对齐)
- `frontend/types/coscientist.ts`(+2 可选字段)
- `frontend/components/coscientist/HypothesisRanking.tsx`(验证徽章)

### 测试(新增)
- `backend/tests/experiment/test_feedback_validation.py`(追加 `TestRunClosure` 3 例)
- `backend/tests/test_rankings_validation_fields.py`(2 例)
- `backend/tests/test_intelligence_contract.py`(3 例)
- `frontend/hooks/useIntelligenceStream.test.tsx`(2 例)
- `frontend/hooks/useUnifiedAgent.test.tsx`(1 例)
- `frontend/components/coscientist/HypothesisRanking.test.tsx`(2 例)

### 测试(既有修复,全量回归对齐)
- `backend/tests/conftest.py`(+22 model import,53 表全注册)
- `backend/tests/services/agent/test_registry.py`/`test_permissions.py`/`test_agent_endpoints.py`(工具数 19→23)
- `backend/tests/test_api_integration.py`/`test_new_endpoints.py`/`test_api_contract.py`/`test_envelope.py`/`test_llm_config.py`(422 契约对齐)
- `backend/tests/test_data.py`/`test_dashboard.py`(`/data/upload` params→data)
- `backend/tests/test_llm_modules_coverage.py`(RAG add_documents AsyncMock)
- `backend/tests/test_efficacy_monitor.py`(_async_db helper)
- `backend/tests/test_agent_enhancements_integration.py`(单例 record try/finally 恢复)
- `backend/tests/test_agent_tool_quality.py`(object.__new__ 绕过单例)
- `backend/tests/test_xenium_neoantigen_parser.py`(asyncio.run)
- `backend/tests/test_modules_extra.py`/`test_opensource_tools.py`/`test_services_integration.py`(mock_mode 状态)
- `backend/tests/test_user_llm_router.py`(agnes-2.5-flash)/`test_optimizers.py`(monitoring_data)
- `backend/tests/coscientist/test_endpoints.py`/`test_embedding_proximity.py`(案例已删/direct_merge)
- `frontend/app/workbench/cells/page.test.tsx`
- `frontend/app/workbench/docking/page.test.tsx`
- `frontend/app/workbench/intelligence/page.test.tsx`(next/navigation mock)
- `frontend/app/workbench/genome/page.test.tsx`(next/navigation mock)
- `frontend/app/workbench/screening/page.test.tsx`(全文重写对齐)
- `frontend/app/workbench/structures/page.test.tsx`(按钮文案对齐)

---

## 七、P2 验收追加(2026-08-07)

### 建议三:邻近度嵌入测试补充
- `backend/tests/coscientist/test_embedding_proximity.py` 追加 `TestSemanticRewriteCapture` 3 例(同义改写捕获/不同主题不合并/通道对比),提交 `bba781a`
- 测试基线:37 passed

### 建议五:三档推理(前端档位选择 UI)
- **设计文档**: `docs/superpowers/specs/2026-08-07-tier-selector-design.md`(`870b716`)
- **实施计划**: `docs/superpowers/plans/2026-08-07-tier-selector.md`(`c00dd86`)
- **后端改动**:
  - `backend/app/schemas/intelligence.py`:ChatRequest 增加 `tier` 字段,ChatResponse 增加 `tier`/`tier_reason`,新增 `TierSuggestRequest`/`TierSuggestResponse`
  - `backend/app/api/v1/endpoints/intelligence.py`:chat 端点透传 tier;新增 `POST /sessions/{id}/suggest-tier` 推荐端点
  - `backend/app/services/intelligence/intent_router.py`:新增 `suggest_tier_detail()` 方法(零成本 keyword 路由 + 档位推荐)
  - `backend/app/services/intelligence/unified_agent_gateway.py`:chat/_chat_via_orchestrator 透传 tier,metadata 增加 tier
- **前端改动**:
  - `frontend/lib/api/intelligence.ts`:新增 `TierChoice` 类型/TierSuggestResponse/suggestTier API/ChatRequest tier
  - `frontend/components/intelligence/TierBar.tsx`:新组件 — segmented control(auto/turbo/standard/deep),显示推荐档位+预估成本
  - `frontend/hooks/useUnifiedAgent.ts`:新增 tier state/setTier,sendMessage 携带 tier,响应 metadata 记录 tier
  - `frontend/app/workbench/intelligence/page.tsx`:渲染 TierBar + 真实成本显示
- **测试**:
  - `backend/tests/test_suggest_tier.py`:3 例(空消息 400/正常推荐返回档位+reason/tier_config 含 max_rounds)
  - `frontend/components/intelligence/TierBar.test.tsx`:4 例
  - `frontend/app/workbench/intelligence/page.test.tsx`:新增 2 例
- **全量验证**:后端 32 passed(test_tier_routing.py) + 3 passed(test_suggest_tier.py);前端 63 files / **604 tests passed**(+5 vs 基线 599)
- 提交:`0f9637f` → `be6216a` → `ff0d3fe` → `3e09388` → `ef681e0` → `c00dd86`,已推送 origin/master
